/**
 * Canonical RushDevs domain. www.rushdevs.com redirects here; the old
 * rush-devs-eta.vercel.app still serves the site directly rather than
 * redirecting, so old links keep working but should not be linked to.
 */
export const RUSHDEVS_URL = 'https://rushdevs.com'
